// Tiro supplies these. Leave whatsappNumber empty until the real number is confirmed:
// the page then runs in preview mode and never sends customers anywhere wrong.
export const config = {
  businessName: 'HandymanKHM',
  // International format, digits only. Example: '27821234567' for 082 123 4567.
  whatsappNumber: '',
};
